#ifndef ENTITY_DATA_GRABBER_H
#define ENTITY_DATA_GRABBER_H

#include <fstream>
#include <vector>

using namespace std;

// this will read an entity data file (.xml) and assign each appropriate value
// to each type of entity. This should happen only upon starting the game.

int extract_entity_data(string file) {
	ifstream ifs(file);

	while()
};




#endif // !ENTITY_DATA_GRABBER_H

